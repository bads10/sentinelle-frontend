# ==============================================================
# Sentinelle Frontend – Variables d'environnement
# Copier ce fichier en .env.local et renseigner les valeurs
# Ne jamais commiter .env.local
# ==============================================================

# URL de l'API backend Sentinelle (FastAPI)
# En développement : http://localhost:8000
# En production : https://api.sentinelle.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optionnel : clé API si le backend est protégé
# NEXT_PUBLIC_API_KEY=

# Optionnel : environnement (development | production)
# NODE_ENV=development
